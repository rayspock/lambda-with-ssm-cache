{
  "name": "lambda-with-ssm-cache",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy": "serverless deploy -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ray",
  "license": "ISC",
  "dependencies": {
    "middy": "^0.30.4"
  },
  "devDependencies": {
    "serverless": "^3.27.0"
  }
}
